# Claude Code entrypoint

@AGENTS.md

Use the project skills in `.claude/skills/` when relevant. Each adapter points to a
canonical procedure in `.agents/skills/`; read that procedure before acting.
Keep personal memory and machine-specific settings outside versioned sources.
